{
  "name": "connected-spaces-platform-web-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:debug": "vitest run --mode debug",
    "test:release": "vitest run --mode release",
    "test:watch:debug": "vitest --mode debug",
    "test:watch:release": "vitest --mode release",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@vitest/browser": "4.1.6",
    "playwright": "1.59.1",
    "typescript": "6.0.3",
    "vitest": "4.1.6"
  }
}
